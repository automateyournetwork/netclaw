# Core MCP framework
mcp
fastmcp

# pyATS (Cisco device automation)
pyats[full]
paramiko

# Common MCP server deps
requests
httpx
pydantic
pydantic-settings
python-dotenv
PyYAML
starlette
uvicorn
aiohttp

# Protocol participation
scapy
networkx

# Packet analysis
# (tshark is system package)

# nmap
python-nmap

# TTS
edge-tts

# Wikipedia
wikipedia

# Subnet calculator (no extra deps beyond mcp/pydantic)

# ISE
aiocache
aiolimiter

# Meraki
meraki

# Infoblox
infoblox-ddi-mcp

# Palo Alto (not compatible with Python 3.13)
# iflow-mcp-cdot65-palo-alto-mcp

# Token optimization
anthropic
toon-format

# SuzieQ MCP is a thin httpx client — does NOT need the full suzieq package
# The suzieq package pins aiohttp==3.6.2 which conflicts with everything

# Batfish MCP is a thin httpx client — pybatfish pulls heavy Java deps
# pybatfish

# gNMI
pygnmi
grpcio
protobuf
cryptography

# Grafana/Prometheus (run via uvx at runtime, but base deps)
prometheus-mcp-server

# CML
cml-mcp

# NSO
cisco-nso-mcp-server

# Itential
itential-mcp

# Infrahub
infrahub-sdk

# Pillow for UML/diagram rendering
pillow
graphviz
