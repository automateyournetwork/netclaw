# Example switch list for job device_snmp (Phase 8).
# Copy ideas into prometheus.yml static_configs or feed render-device-snmp-scrape.py
#
# community is NOT stored here — see snmp.yml auth.

targets:
  - name: HomeSwitch01
    ip: 192.168.3.2
    role: switch
  - name: HomeSwitch02
    ip: 192.168.3.3
    role: switch
  - name: HomeSwitch04
    ip: 192.168.3.5
    role: switch
