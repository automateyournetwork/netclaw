# Legacy / pilot dashboards (not provisioned)

These JSON files were ported from k3s-observability-stack or earlier Phase 8
work. They are **kept for reference** but are **not** loaded by Grafana.

Active suite (provisioned from `../json/`):

| Board | UID |
|-------|-----|
| Network | `convergence-network` |
| Security | `convergence-security` |
| NetClaw | `convergence-netclaw` |

See `deploy/convergence/grafana/README.md`.
